name: gps_tracker
description: High-performance GPS Speed Tracker with AI Coach for iOS and Web.
publish_to: "none"
version: 1.2.0+3

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.1
  permission_handler: ^11.3.1
  flutter_map: ^8.0.0
  latlong2: ^0.9.1
  apple_maps_flutter: ^1.4.0
  fl_chart: ^0.71.0
  shared_preferences: ^2.3.5
  intl: ^0.20.2
  http: ^1.3.0
  flutter_markdown: ^0.7.3+1
  cupertino_icons: ^1.0.8
  wakelock_plus: ^1.2.11
  supabase_flutter: ^2.0.0
  flutter_compass: ^0.8.1
  battery_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.9
  hive_generator: ^2.0.1
  flutter_lints: ^5.0.0

flutter:
<<<<<<< HEAD
  uses-material-design: true
